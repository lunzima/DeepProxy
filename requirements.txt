fastapi>=0.136.0
uvicorn[standard]>=0.48.0
litellm>=1.83.7
pyyaml>=6.0.3
pydantic>=2.12.0          # litellm 1.83.7 把 pydantic 钉死在 ==2.12.5，floor 留弹性
pydantic-settings>=2.14.0
httpx>=0.28.1
beautifulsoup4>=4.14.0
lxml>=6.1.0

# === BERT 路由器（可选，CPU 推理，无 GPU 依赖） ===
# 安装命令（三选一）：
#   CPU（推荐）: pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install transformers peft datasets
#   CUDA 12.1:  pip install torch --index-url https://download.pytorch.org/whl/cu121 && pip install transformers peft datasets
#   ROCm 6.2:   pip install torch --index-url https://download.pytorch.org/whl/rocm6.2 && pip install transformers peft datasets
# 不安装也可运行——路由器自动降级到纯规则模式，不影响服务。
